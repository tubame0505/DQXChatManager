---
phase: 03-behavior-preservation-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/phases/03-behavior-preservation-and-validation/03-VALIDATION.md
  - .planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md
autonomous: false
requirements:
  - BEHV-01
  - BEHV-02
  - BEHV-03
  - SECU-03
  - VALD-01
  - VALD-02
must_haves:
  truths:
    - "Windows の実行環境で login smoke が既存の driver path を使って成立するか、失敗なら regression として切り出せる。"
    - "export と import は別々に smoke 結果が記録され、片方だけの失敗も識別できる。"
    - "Phase 3 verification artifact は compile/build gates と manual smoke の両方を requirement 単位で判定できる。"
  artifacts:
    - path: .planning/phases/03-behavior-preservation-and-validation/03-VALIDATION.md
      provides: "login/export/import の個別 smoke evidence と regression boundary notes"
      contains: "separate login/export/import results"
    - path: .planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md
      provides: "Phase 3 must-have verification report"
      contains: "truths, requirement coverage, pass/fail readout"
  key_links:
    - from: .planning/phases/03-behavior-preservation-and-validation/03-VALIDATION.md
      to: .planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md
      via: "manual smoke outcomes summarized into phase verification"
      pattern: "login|export|import"
    - from: src/dqx-hiroba.ts
      to: src/services/emote-manager.ts
      via: "exportEmote()/importEmote() runtime orchestration"
      pattern: "exportEmote\\(|importEmote\\("
    - from: src/dqx-hiroba.ts
      to: src/services/webdriver-session.ts
      via: "login() -> initialize() -> navigateToTarget()"
      pattern: "initialize\\(|navigateToTarget\\("
---

<objective>
Automated evidenceの上に Windows runtime smoke と最終 verification report を積み上げ、Phase 3 の behavior-preservation requirements を pass/fail で閉じる。

Purpose: D-04, D-05, D-06, D-10 に従い、login/export/import を個別に観測し、失敗時は修正設計へ膨らませず regression boundary として固定する。
Output: `03-VALIDATION.md` の manual smoke sections と `.planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md`。
</objective>

<execution_context>
@C:/Users/yukis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/yukis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-behavior-preservation-and-validation/03-CONTEXT.md
@.planning/phases/03-behavior-preservation-and-validation/03-01-PLAN.md
@.planning/phases/03-behavior-preservation-and-validation/03-VALIDATION.md
@.planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md
@.planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md
@.planning/codebase/TESTING.md
@src/main.ts
@src/dqx-hiroba.ts
@src/services/webdriver-session.ts
@src/services/emote-manager.ts

<interfaces>
From src/dqx-hiroba.ts:
```typescript
login(driverPath: string, profile: string): Promise<void>;
exportEmote(): Promise<ProcessingResult>;
importEmote(emote: string): Promise<ProcessingResult>;
```

From src/services/webdriver-session.ts:
```typescript
initialize(driverPath: string, profilePath?: string): Promise<void>;
navigateToTarget(): Promise<void>;
isInitialized(): boolean;
getDriver(): webdriver.ThenableWebDriver;
```

From src/services/emote-manager.ts:
```typescript
exportEmote(): Promise<ProcessingResult>;
importEmote(emoteSettings: string): Promise<ProcessingResult>;
validateEmoteData(emoteData: EmoteData): boolean;
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run separate Windows smoke checks for login, export, and import</name>
  <files>.planning/phases/03-behavior-preservation-and-validation/03-VALIDATION.md</files>
  <action>Using the artifact from Plan 03-01, append a dedicated manual smoke section to `03-VALIDATION.md` with separate result blocks for `login`, `export`, and `import` per D-04 and D-05. The smoke run must use the existing Windows-targeted `npm run dev` flow only per D-06. For `login`, confirm whether the app launches the cached or downloaded EdgeDriver path and reaches the target login browser state through the existing orchestration; for `export`, confirm the existing export flow still runs after login; for `import`, confirm the existing import flow still runs after login and confirmation dialog handling is unchanged. If any flow regresses, capture the observed boundary, blocking symptom, and affected requirement in the artifact, and stop at that failure instead of proposing fixes or adding new validation scope per D-10.</action>
  <what-built>Phase 3 automated evidence in `03-VALIDATION.md` and the preserved runtime code paths for login, export, and import.</what-built>
  <how-to-verify>
    1. On Windows, start the app with `npm run dev`.
    2. Execute the normal login flow and record whether Edge launches through the existing WebDriver path, including whether an existing cached driver was reused or a matching driver download occurred.
    3. After login succeeds, execute the normal export flow and record its result independently from login.
    4. After login succeeds, execute the normal import flow with representative existing emote data and record its result independently from login and export.
    5. If any flow fails, write the failure boundary into `03-VALIDATION.md` under the corresponding `login`, `export`, or `import` heading and do not broaden the phase into a fix plan.
  </how-to-verify>
  <verify>
    <automated>powershell -NoProfile -Command "if (!(Test-Path .planning\\phases\\03-behavior-preservation-and-validation\\03-VALIDATION.md)) { exit 1 }; $content = Get-Content .planning\\phases\\03-behavior-preservation-and-validation\\03-VALIDATION.md -Raw; if ($content -notmatch 'npm run compile' -or $content -notmatch 'npm run build' -or $content -notmatch 'contextIsolation' -or $content -notmatch 'nodeIntegration') { exit 1 }"</automated>
  </verify>
  <done>`03-VALIDATION.md` contains three separate smoke outcomes (`login`, `export`, `import`) with enough detail to localize regressions to `BEHV-01`, `BEHV-02`, or `BEHV-03` instead of a single combined note.</done>
  <resume-signal>Type "approved" if login/export/import all pass, or reply with the observed regression for the failing flow.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Produce the final Phase 3 verification report</name>
  <files>.planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md</files>
  <action>Create `.planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md` after Task 1 using the same verification style as Phase 2, and summarize the must-haves, artifacts, key links, and requirement coverage for `BEHV-01`, `BEHV-02`, `BEHV-03`, `SECU-03`, `VALD-01`, and `VALD-02`. Pull compile/build outcomes, explicit `contextIsolation` / `nodeIntegration` evidence, and the separate `login` / `export` / `import` smoke results from `03-VALIDATION.md`. If a regression was recorded, mark the affected truth and requirement as failed with the captured boundary and avoid scope expansion; the report should describe what failed, not how to redesign it. Keep generated outputs excluded from manual editing and note that Phase 3 closes on evidence, not on any new runtime implementation.</action>
  <verify>
    <automated>powershell -NoProfile -Command "if (!(Test-Path .planning\\phases\\03-behavior-preservation-and-validation\\03-VERIFICATION.md)) { exit 1 }; $content = Get-Content .planning\\phases\\03-behavior-preservation-and-validation\\03-VERIFICATION.md -Raw; foreach ($pattern in @('BEHV-01','BEHV-02','BEHV-03','SECU-03','VALD-01','VALD-02','npm run compile','npm run build','login','export','import')) { if ($content -notmatch [regex]::Escape($pattern)) { exit 1 } }"</automated>
  </verify>
  <done>`03-VERIFICATION.md` gives a requirement-by-requirement Phase 3 readout that a checker can use directly, including explicit pass/fail evidence for compile, build, login, export, import, and the Electron security settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human smoke test → runtime behavior claim | Manual observation is the only proof for browser-driven login/export/import behavior |
| Validation artifact → verification report | The final report is only as good as the source evidence it summarizes |
| Runtime failure → phase scope | Regressions must be recorded without silently turning validation into redesign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | R | manual smoke evidence in `03-VALIDATION.md` | mitigate | Record `login`, `export`, and `import` as separate checks per D-04 and D-05 with concrete observed outcomes |
| T-03-05 | T/R | runtime regression handling | mitigate | Per D-10, capture failures as bounded regressions with affected requirement IDs instead of broadening into fixes during validation |
| T-03-06 | R | `.planning/phases/03-behavior-preservation-and-validation/03-VERIFICATION.md` | mitigate | Build the verification report directly from `03-VALIDATION.md` evidence and preserve pass/fail status for each requirement |
</threat_model>

<verification>
Automated prerequisite: `npm run compile` and `npm run build` evidence from Plan 03-01 must already exist. Manual prerequisite: Windows `npm run dev` smoke run must produce separate `login`, `export`, and `import` outcomes in `03-VALIDATION.md`. Final artifact check: `03-VERIFICATION.md` exists and maps every Phase 3 requirement to evidence.
</verification>

<success_criteria>
Phase 3 closes with a single validation artifact containing required gates and separate smoke outcomes, plus a verification report that explicitly marks `BEHV-01`, `BEHV-02`, `BEHV-03`, `SECU-03`, `VALD-01`, and `VALD-02` as passed or failed based on evidence only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-behavior-preservation-and-validation/03-02-SUMMARY.md`
</output>

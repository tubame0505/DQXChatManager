---
phase: 02-dependency-and-packaging-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md
autonomous: true
requirements:
  - DEPS-03
  - VALD-03
must_haves:
  truths:
    - "active boundary (`src/`, `package.json`, `package-lock.json`, `AGENTS.md`, active scripts) で `regedit` / VBS build dependency が検索に出ない。"
    - "history/generated files を除外した根拠が validation artifact に明記され、false positive で fail しない。"
    - "Phase 2 が runtime behavior を再オープンしていないことを検証証跡として説明できる。"
  artifacts:
    - path: .planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md
      provides: "Phase 2 の scoped audit と dependency cleanup validation evidence"
      contains: "rg search results and boundary notes"
  key_links:
    - from: .planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md
      to: package.json
      via: "cleanup 後 build script / dependency state の記録"
      pattern: "npm uninstall|build"
    - from: .planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md
      to: package-lock.json
      via: "lockfile 収束と scoped search の記録"
      pattern: "package-lock.json|rg -n"
---

<objective>
Phase 2 cleanup の結果を active-reference boundary に限定した検証証跡として固定し、`VALD-03` を historical/generative noise なしで閉じる。

Purpose: dependency removal 自体と、その証明方法を分離して後続 checker が false positive なく検証できるようにする。
Output: `.planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md` に残る scoped search・lockfile/manifest 整合・scope boundary の記録。
</objective>

<execution_context>
@C:/Users/yukis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/yukis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-packaging-cleanup/02-CONTEXT.md
@.planning/phases/02-dependency-and-packaging-cleanup/02-RESEARCH.md
@.planning/phases/02-dependency-and-packaging-cleanup/02-01-PLAN.md
@package.json
@package-lock.json
@AGENTS.md
@src/deploy.md
@scripts/wait-for-file.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record a boundary-scoped active-reference audit for `VALD-03`</name>
  <files>.planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md</files>
  <action>`.planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md` を新規作成し、D-08 と D-09 に沿う scoped audit を記録する。少なくとも `src/`, `scripts/`, `package.json`, `package-lock.json`, `AGENTS.md`, `src/deploy.md` を active boundary として検索し、`regedit`, `dist/vbs`, `.vbs`, `VBS`, `cpx2`, `cpx` の残存有無を記録すること。`.planning/`, `dist/`, `packages/`, `.npm-cache/`, `tsconfig.tsbuildinfo` は false-positive 除外対象として明記し、そこで見つかる historical/generated references は failure に数えない。repo-wide grep を pass/fail 基準に使わず、boundary-scoped command とその結果を証跡として残す。</action>
  <verify>
    <automated>rg -n "regedit|dist/vbs|\\.vbs|\\bVBS\\b|cpx2|\\bcpx\\b" package.json package-lock.json AGENTS.md src scripts</automated>
  </verify>
  <done>`02-VALIDATION.md` に active boundary と除外境界、実行コマンド、結果要約が揃い、`VALD-03` を repo history ノイズと切り分けて判定できる。</done>
</task>

<task type="auto">
  <name>Task 2: Document manifest-lockfile convergence and preserved phase boundary</name>
  <files>.planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md</files>
  <action>`02-VALIDATION.md` に、cleanup が runtime behavior changes ではなく package/build metadata cleanup であることを追記し、D-01, D-02, D-06, D-07, D-10 のトレースを残す。`package.json` と `package-lock.json` から `regedit` / `cpx2` が消えていること、`AGENTS.md` が新しい `build` 説明へ揃っていること、`src/driver_downloader.ts` など Phase 1 runtime files はこの phase の差分対象外であることを明記すること。必要なら `git diff --name-only` の結果を引用してもよいが、unrelated worktree noise を fail 条件にしない。checker が requirement coverage を追えるように `DEPS-03` と `VALD-03` の対応関係を artifact 内で列挙する。</action>
  <verify>
    <automated>powershell -NoProfile -Command "$pkg = Get-Content package.json | ConvertFrom-Json; if (($pkg.dependencies.PSObject.Properties.Name -contains 'regedit') -or ($pkg.devDependencies.PSObject.Properties.Name -contains 'cpx2')) { exit 1 }; rg -n 'regedit|cpx2' package-lock.json; if ($LASTEXITCODE -eq 0) { exit 1 }"</automated>
  </verify>
  <done>`02-VALIDATION.md` が scoped search の結果だけでなく、manifest/lockfile 収束と runtime scope preservation を説明する Phase 2 の正式な validation artifact になっている。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation artifact → repository truth | 記録された検索・差分要約が実際の active state を正しく表す必要がある |
| Active boundary → historical/generated files | pass/fail 対象と参照ノイズを切り分ける境界 |
| Phase 2 verification → runtime behavior | cleanup 検証が Phase 1/3 の behavior checks に逸脱しない境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | R | `02-VALIDATION.md` search evidence | mitigate | D-08/D-09 の boundary を本文に固定し、実行コマンドをそのまま残して判定根拠を再現可能にする |
| T-02-05 | R | generated/history false positives | mitigate | `.planning/`, `dist/`, `packages/`, `.npm-cache/`, `tsconfig.tsbuildinfo` を除外対象として明記し、repo-wide grep を pass/fail に使わない |
| T-02-06 | T | runtime scope creep during verification | mitigate | validation artifact で Phase 2 非対象の runtime files を列挙し、behavior preservation の再検証は Phase 3 へ委譲すると明記する |
</threat_model>

<verification>
Scoped command `rg -n "regedit|dist/vbs|\.vbs|\bVBS\b|cpx2|\bcpx\b" package.json package-lock.json AGENTS.md src scripts` が active boundary でヒットしないこと。`02-VALIDATION.md` に除外境界と requirement trace (`DEPS-03`, `VALD-03`) が残ること。
</verification>

<success_criteria>
`VALD-03` の証跡が `.planning/phases/02-dependency-and-packaging-cleanup/02-VALIDATION.md` に残り、checker が active files のみを対象に `regedit` / VBS dependency chain の不在を確認できる。
</success_criteria>

<output>
After completion, create `.planning/phases/02-dependency-and-packaging-cleanup/02-02-SUMMARY.md`
</output>

---
phase: 01-vbs-free-edge-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md
autonomous: false
requirements:
  - EDGE-03
  - SECU-01
  - SECU-02
must_haves:
  truths:
    - "Edge discovery failure modes that should be recoverable are observed as controlled failures, not crashes."
    - "Security boundary violations still surface as exceptional handling."
    - "Windows login can be smoke-tested without introducing new registry scopes or VBS helper assumptions."
  artifacts:
    - path: .planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md
      provides: "Compile/build/smoke validation evidence for Phase 1"
      contains: "command results and manual verification notes"
  key_links:
    - from: src/main.ts
      to: src/driver_downloader.ts
      via: "login IPC calling getBrowserVersion()"
      pattern: "getBrowserVersion\\("
    - from: .planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md
      to: src/driver_downloader.ts
      via: "recorded evidence of bounded discovery behavior"
      pattern: "powershell.exe|SecurityError|undefined"
---

<objective>
Phase 1 の実装が公開契約と security boundary を壊していないことを、実行可能な validation artifact と Windows smoke check で確認する。

Purpose: implementation 完了後の regressions を Phase 1 内で切り分けやすくし、Phase 2 cleanup と混同しない検証証跡を残す。
Output: `.planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md` と、compile/build/manual smoke の結果。
</objective>

<execution_context>
@C:/Users/yukis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/yukis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vbs-free-edge-discovery/01-CONTEXT.md
@.planning/phases/01-vbs-free-edge-discovery/01-RESEARCH.md
@.planning/phases/01-vbs-free-edge-discovery/01-01-PLAN.md
@src/driver_downloader.ts
@src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record automated validation evidence for the bounded discovery change</name>
  <files>.planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md</files>
  <action>`.planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md` を新規作成し、Phase 1 専用の検証証跡を残す。少なくとも次を含めること: `npm run compile` の結果、`npm run build` の結果、`src/driver_downloader.ts` が `powershell.exe` + 固定引数配列を使っていること、registry scope が allowed Edge App Paths key のみであること、`SecurityError` が exceptional path として温存されていること。ここで `npm run build` は Phase 1 の pass/fail gate ではなく観測証跡であり、Phase 2 前なので VBS cleanup 未実施による結果は failure 条件にしない、と明記すること。repo-wide regedit cleanup や dist/vbs cleanup は検証対象に含めない。</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>`01-VALIDATION.md` に automated validation の結果と、Phase 1 と Phase 2 の境界が明記されている。Phase 1 の security/behavior contract に必要な証跡が後続レビューで読める形で残る。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the Windows login smoke check against the new discovery path</name>
  <files>.planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md, src/driver_downloader.ts, src/main.ts</files>
  <action>Task 1 で作成した `01-VALIDATION.md` を参照しながら、Windows 上の実アプリ login flow で bounded PowerShell discovery が UI/driver orchestration を壊していないか確認する。Phase 1 では App Paths registry location 以外を探しにいかないこと、`regedit` / VBS helper に戻っていないこと、recoverable failure は useful log + 停止、`SecurityError` は exceptional path として扱われ続けることを確認対象に含める。</action>
  <what-built>`DriverDownloader.getBrowserVersion()` の bounded PowerShell discovery 実装と、それを使う `login` IPC の継続動作。</what-built>
  <how-to-verify>
    1. Windows 環境で launch/setup として `npm run dev` を起動する。
    2. アプリの login 操作を実行し、PowerShell/VBS helper のポップアップや `regedit` 依存エラーが出ないことを確認する。
    3. Edge が通常どおり起動し、driver 未配置なら従来どおり matching EdgeDriver download が進むことを確認する。
    4. Edge 未検出または version extraction failure を再現できる環境があれば、アプリがクラッシュせず useful log を残して停止することを確認する。
    5. `SecurityError` を意図的に再現できる変更は加えず、code review と `01-VALIDATION.md` の証跡で exceptional path 維持を確認する。
  </how-to-verify>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>Windows login smoke check の結果が `approved` か具体的な failure として返り、bounded PowerShell discovery が login orchestration を壊していないか判定できる。</done>
  <resume-signal>Type "approved" if the Windows smoke check passes, or reply with the observed failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation commands → source contract | compile/build と smoke check が implementation contract を間接確認する |
| Human smoke test → Electron login flow | 実アプリ login 操作で main process orchestration を確認する |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | R | `.planning/phases/01-vbs-free-edge-discovery/01-VALIDATION.md` | mitigate | compile/build 結果と boundary checks を記録し、後続レビューで failure mode を追跡可能にする |
| T-01-06 | T | Windows smoke test expectations | mitigate | verification 手順で App Paths scope widening と VBS cleanup 越境を除外し、Phase 1 contract のみを確認する |
| T-01-07 | R | `src/main.ts` login integration | mitigate | human-verify で `getBrowserVersion()` failure path と normal login path の双方を確認する |
</threat_model>

<verification>
Automated gate: `npm run compile` が通り、その結果が `01-VALIDATION.md` に記録されること。 Evidence-only: `npm run build` の結果は `01-VALIDATION.md` に記録するが、Phase 1 の pass/fail gate にはしないこと。 Manual: Windows で `npm run dev` を launch/setup として起動し、login smoke test を実施する。
</verification>

<success_criteria>
Phase 1 の bounded discovery 実装が `npm run compile` と Windows login smoke で discovery contract を維持し、`regedit`/VBS helper 前提の回帰が見えない。`npm run build` は証跡として記録されるが、Phase 2 cleanup 未実施を理由に Phase 1 を fail と判定しない validation artifact が残る。
</success_criteria>

<output>
After completion, create `.planning/phases/01-vbs-free-edge-discovery/01-02-SUMMARY.md`
</output>

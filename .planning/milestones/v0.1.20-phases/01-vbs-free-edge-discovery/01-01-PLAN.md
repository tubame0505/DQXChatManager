---
phase: 01-vbs-free-edge-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/driver_downloader.ts
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02
  - EDGE-03
  - SECU-01
  - SECU-02
must_haves:
  truths:
    - "Windows では regedit を使わずに Edge 実行ファイルパスを取得できる。"
    - "取得した Edge 実行ファイルから ProductVersion を読み取れる。"
    - "回復可能な検出失敗は undefined を返し、SecurityError は例外のまま残る。"
    - "レジストリ照会先と PowerShell 呼び出し方法が固定され、探索境界が広がらない。"
  artifacts:
    - path: src/driver_downloader.ts
      provides: "PowerShell ベースの Edge App Paths / ProductVersion discovery"
      contains: "execFile(\"powershell.exe\", fixedArgs, options)"
  key_links:
    - from: src/driver_downloader.ts
      to: src/security/security-validator.ts
      via: "RegistryValidator.validateRegistryPath()"
      pattern: "validateRegistryPath"
    - from: src/driver_downloader.ts
      to: src/main.ts
      via: "getBrowserVersion() public contract"
      pattern: "Promise<string \\| undefined>"
---

<objective>
`DriverDownloader` の Edge discovery を `regedit` 依存から PowerShell ベースへ差し替え、公開契約を壊さずに Phase 1 の要件を満たす。

Purpose: VBS/Windows Script Host helper を必要とする discovery 経路を Phase 1 の範囲だけで除去する。
Output: `src/driver_downloader.ts` の bounded PowerShell discovery 実装と、失敗分類を維持した `getBrowserVersion()`。
</objective>

<execution_context>
@C:/Users/yukis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/yukis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vbs-free-edge-discovery/01-CONTEXT.md
@.planning/phases/01-vbs-free-edge-discovery/01-RESEARCH.md
@src/driver_downloader.ts
@src/security/security-validator.ts
@src/config/app-config.ts
@src/main.ts

<interfaces>
From src/security/security-validator.ts:
```typescript
export class SecurityError extends Error {
    constructor(
        message: string,
        public readonly code: string = "SECURITY_VIOLATION"
    );
}

export class RegistryValidator {
    static validateRegistryPath(regPath: string): string;
}
```

From src/config/app-config.ts:
```typescript
export const APP_CONFIG = {
    REGISTRY: {
        EDGE_PATH:
            "HKLM\\Software\\Microsoft\\Windows\\CurrentVersion\\App Paths\\msedge.exe",
    },
} as const;
```

From src/main.ts:
```typescript
const edgeVersion = await downloader.getBrowserVersion();
if (!edgeVersion) {
    logger.error("Edgeバージョンの取得に失敗しました");
    return;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace regedit lookup with bounded PowerShell path discovery</name>
  <files>src/driver_downloader.ts</files>
  <action>`src/driver_downloader.ts` から `regedit` import と `regedit.list()` ベースの実装を削除し、D-01, D-02, D-03, D-06, D-07 に従う private helper 群へ置き換える。`RegistryValidator.validateRegistryPath()` を PowerShell 実行前に必ず通し、問い合わせ先は `HKLM\Software\Microsoft\Windows\CurrentVersion\App Paths\msedge.exe` だけに限定すること。PowerShell 起動は `child_process.execFile("powershell.exe", [...])` の固定 executable 名と固定引数配列 `-NoProfile`, `-NonInteractive`, `-ExecutionPolicy`, `Bypass`, `-Command` を使い、`shell: true` や `exec()` は使わないこと。`-Command` に渡す文字列へ埋め込む可変値は、検証済みレジストリパスまたは system lookup で得た Edge executable path のみとし、PowerShell の単一引用符リテラル用エスケープ helper を実装して Windows path の空白・引用符バグを防ぐこと。固定パス probing や HKCU fallback は追加しない。</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>`DriverDownloader` が `regedit` を参照せず、単一の bounded PowerShell query で Edge 実行ファイルパスを取得できる。PowerShell 呼び出しは固定 executable 名・固定引数配列・単一引用符エスケープ helper を通る形に統一されている。</done>
</task>

<task type="auto">
  <name>Task 2: Preserve version lookup and failure classification contract</name>
  <files>src/driver_downloader.ts</files>
  <action>`getBrowserVersion()` の公開シグネチャと `src/main.ts` から見た振る舞いを維持しつつ、D-04, D-05, D-08, D-09 を実装する。Windows 以外では従来どおり debug log + `undefined` を返し、Windows では Task 1 の helper で Edge path を取得してから `VersionInfo.ProductVersion` を読む PowerShell query を実行する。registry lookup failure、empty stdout、version extraction failure はそれぞれ原因が分かる log を残して `undefined` を返す一方、`SecurityError` は security violation として log して再 throw すること。既存の driver download/cache 流れや `getDriver()` の URL 構築には手を広げず、Phase 2 の dependency cleanup も行わない。</action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>`getBrowserVersion()` は unsupported platform または recoverable discovery failure で `undefined` を返し続け、`SecurityError` だけは例外のまま外へ出る。検出した version は従来どおり `getDriver()` に渡せる文字列で、driver download/cache の既存フローは変更されていない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Electron main → powershell.exe | Main process から OS コマンド実行へ制御が渡る |
| DriverDownloader → Windows registry | allowlist 済み registry path だけを query すべき境界 |
| DriverDownloader → file metadata | registry から得た executable path を version query に渡す境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T/E | `src/driver_downloader.ts` PowerShell runner | mitigate | `execFile("powershell.exe", fixedArgs, options)` を使い、`exec()`/`shell: true` を禁止する |
| T-01-02 | T | `src/driver_downloader.ts` registry query scope | mitigate | `RegistryValidator.validateRegistryPath()` を query 前に必ず通し、D-02 per allowed App Paths key only を実装する |
| T-01-03 | T | `src/driver_downloader.ts` PowerShell literal construction | mitigate | PowerShell 単一引用符 literal helper を実装し、validated path のみ埋め込む |
| T-01-04 | R | `getBrowserVersion()` error handling | mitigate | `SecurityError` は log + rethrow、recoverable failures は個別 log + `undefined` に分岐し、D-09 を守る |
</threat_model>

<verification>
`npm run compile` が通ること。実装差分の目視確認では `src/driver_downloader.ts` に `regedit` import/use が残っておらず、PowerShell 呼び出しが `powershell.exe` + 固定引数配列へ統一されていること。
</verification>

<success_criteria>
Phase 1 の implementation scope だけで `EDGE-01`, `EDGE-02`, `EDGE-03`, `SECU-01`, `SECU-02` を満たす差分が `src/driver_downloader.ts` に収まり、`getBrowserVersion()` の公開契約と `main.ts` 連携が保たれている。
</success_criteria>

<output>
After completion, create `.planning/phases/01-vbs-free-edge-discovery/01-01-SUMMARY.md`
</output>

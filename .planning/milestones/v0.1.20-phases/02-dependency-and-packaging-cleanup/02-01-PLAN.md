---
phase: 02-dependency-and-packaging-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - AGENTS.md
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
must_haves:
  truths:
    - "`regedit` が runtime dependency から外れ、lockfile でも top-level 依存として残らない。"
    - "`npm run build` が `node_modules/regedit/vbs/*` を `dist/vbs` へコピーしない。"
    - "active な build/package guidance は cleanup 後の実際の挙動に一致し、VBS copy 前提を案内しない。"
  artifacts:
    - path: package.json
      provides: "依存宣言と build script の cleanup"
      contains: "\"build\": \"cross-env NODE_ENV=\\\"production\\\" webpack --progress\""
    - path: package-lock.json
      provides: "npm uninstall により再収束した lockfile"
      contains: "\"packages\""
    - path: AGENTS.md
      provides: "cleanup 後の active build guidance"
      contains: "npm run build"
  key_links:
    - from: package.json
      to: package-lock.json
      via: "npm uninstall regedit cpx2 --save"
      pattern: "regedit|cpx2"
    - from: package.json
      to: AGENTS.md
      via: "build script と contributor guidance の整合"
      pattern: "npm run build"
---

<objective>
`regedit` / VBS packaging edge を manifest・lockfile・active build guidance から除去し、Phase 2 の cleanup を runtime へ波及させずに完了できる状態を作る。

Purpose: Phase 1 で不要になった dependency chain を package manager 正本で切り、以後の build/package metadata が VBS helper shipping を前提にしないようにする。
Output: `package.json` の build script と dependency 宣言の cleanup、`package-lock.json` の収束、`AGENTS.md` の active guidance 更新。
</objective>

<execution_context>
@C:/Users/yukis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/yukis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dependency-and-packaging-cleanup/02-CONTEXT.md
@.planning/phases/02-dependency-and-packaging-cleanup/02-RESEARCH.md
@package.json
@package-lock.json
@AGENTS.md
@src/deploy.md

<interfaces>
From package.json:
```json
{
  "scripts": {
    "build": "cross-env NODE_ENV=\"production\" webpack --progress && cpx node_modules/regedit/vbs/* dist/vbs",
    "buildwin": "electron-packager . DQXChatManager --platform=win32 --arch=x64 --overwrite --ignore='src' --ignore 'node_modules' --ignore='.vscode' --ignore='media' --out=packages"
  },
  "dependencies": {
    "regedit": "^5.1.4"
  },
  "devDependencies": {
    "cpx2": "^8.0.2"
  }
}
```

From AGENTS.md:
```md
- `npm run build`: creates a production `dist/` bundle and copies required `regedit` VBS files.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove `regedit` and orphaned `cpx2` through npm as the source of truth</name>
  <files>package.json, package-lock.json</files>
  <action>`package.json` / `package-lock.json` の依存除去は手編集ではなく `npm uninstall regedit cpx2 --save` を authoritative path として実行し、D-01, D-03, D-04, D-05 を実装する。実行前に active boundary で `cpx` usage を確認し、`package.json` の build script 以外に `cpx2` の利用がないことを確かめたうえで削除すること。削除後は `package.json` と `package-lock.json` の両方を見て `regedit` / `cpx2` の top-level 宣言と解決エントリが消えていることを確認し、lockfile を手で編集しないこと。`dist/`, `packages/`, `.npm-cache/`, `tsconfig.tsbuildinfo` は D-02 により未編集のままにし、`src/driver_downloader.ts` や runtime behavior には触れない。</action>
  <verify>
    <automated>powershell -NoProfile -Command "$pkg = Get-Content package.json | ConvertFrom-Json; if (($pkg.dependencies.PSObject.Properties.Name -contains 'regedit') -or ($pkg.devDependencies.PSObject.Properties.Name -contains 'cpx2')) { exit 1 }"</automated>
  </verify>
  <done>`package.json` から `regedit` と `cpx2` が消え、`package-lock.json` も npm により再生成された依存グラフへ収束している。依存除去の正本操作が `npm uninstall` であることが差分から明確で、runtime source には変更が入っていない。</done>
</task>

<task type="auto">
  <name>Task 2: Delete the VBS copy step and align active build guidance</name>
  <files>package.json, AGENTS.md</files>
  <action>`package.json` の `build` script から `&& cpx node_modules/regedit/vbs/* dist/vbs` を削除して D-03 を満たし、その結果 `AGENTS.md` の `npm run build` 説明も cleanup 後の事実へ更新する。D-01 と D-05 に従い、新しい copy helper や build refactor は導入せず、webpack と `buildwin` の既存役割はそのまま残すこと。`src/deploy.md` は active boundary の監査対象として確認するが、`regedit` / VBS を案内していない現状なら未変更のままにする。文言更新では historical な `.planning/` artifacts を書き換えず、active guidance のみを対象にする。</action>
  <verify>
    <automated>rg -n "regedit|dist/vbs|\\bcpx\\b|cpx2|\\.vbs|\\bVBS\\b" package.json AGENTS.md</automated>
  </verify>
  <done>`npm run build` は production webpack build だけを実行する script になり、`AGENTS.md` も VBS copy 前提を含まない。cleanup は manifest/build guidance の範囲に留まり、runtime behavior を再設計していない。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm CLI → manifest/lockfile | package manager が依存グラフと lockfile を再書き換えする境界 |
| package.json → contributor guidance | 実際の build script と文書化された手順を一致させる境界 |
| Phase 2 cleanup → runtime source | 依存 cleanup が Phase 1 の runtime behavior に波及しないよう保つ境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `package.json` / `package-lock.json` | mitigate | `npm uninstall regedit cpx2 --save` を正本操作にし、manifest と lockfile を同時更新させる。lockfile 手編集は禁止する |
| T-02-02 | R | `AGENTS.md` build guidance | mitigate | `package.json` の `build` script と同じ wave で文言を更新し、cleanup 後の手順だけを案内する |
| T-02-03 | T | Phase 2 scope boundary | mitigate | Task action で `src/driver_downloader.ts` など runtime files 非対象を明示し、D-10 により runtime behavior を再オープンしない |
</threat_model>

<verification>
`npm uninstall regedit cpx2 --save` 実行後、`package.json` から両依存が消えていること。`package.json` と `AGENTS.md` に対する scoped search で `regedit` / `VBS` / `dist/vbs` / `cpx` が残らないこと。
</verification>

<success_criteria>
`DEPS-01`, `DEPS-02`, `DEPS-03` に必要な差分が `package.json`, `package-lock.json`, `AGENTS.md` に限定され、build script と依存グラフから `regedit` / VBS packaging path が除去されている。
</success_criteria>

<output>
After completion, create `.planning/phases/02-dependency-and-packaging-cleanup/02-01-SUMMARY.md`
</output>
